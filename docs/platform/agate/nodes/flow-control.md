# Flow control nodes

**Flow control nodes** shape how data moves through a [flow](../flows.md) rather than extracting or saving anything themselves. They're useful when a flow branches into several paths that you later need to bring back together.

| Node | What it does |
| --- | --- |
| **Gather** | Combines the results of several upstream steps into a single, organized bundle |
